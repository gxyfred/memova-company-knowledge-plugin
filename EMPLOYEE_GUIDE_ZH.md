# Memova 公司知识助手：员工安装与使用指南

适用对象：现在参加公司知识助手安装、使用和真实测试的所有 Memova 在职员工

当前最低版本：`memova-company-knowledge 0.4.4`

这份指南专门写给第一次使用 Codex、不了解 GitHub、Plugin、MCP 或 OAuth 的同事。正常情况下，
你只需要复制几段固定话术给自己的 Codex，并在 Microsoft 页面登录自己的 Memova 工作账号。

当前阶段允许所有员工按本指南安装和参与测试。后续 S13-04 会根据真实反馈调整指南并进行正式的
覆盖校验；这不妨碍员工现在开始安装、查询和本人委托提交测试。

## 一、它能做什么

公司知识助手有两个主要用途：

1. 查询公司公共知识，并返回来源、证据快照时间、适用范围和已知限制；
2. 把你确认过的、适合公司内部公开的新事实或工作结论发布到公司公共知识库。

每位在职员工使用相同的查询和提交入口。职位、部门和过去负责的工作不会限制你可以查询或提交
哪一类知识；系统会根据当前登录身份、来源访问权和统一的内容安全规则处理请求。所有查询和发布
都可追溯到实际操作人。

## 二、每位员工要完成的标准测试

每位员工按本文后续步骤依次完成：

1. 安装或升级公司知识助手；
2. 使用自己的 Memova Microsoft 工作账号登录；
3. 彻底重启 Codex，在新任务中完成入职自检；
4. 用正常工作语言查询至少一个公司问题，并检查来源、快照、范围和限制；
5. 使用一条本人确认、无敏感信息的事实完成预览、最终确认和发布；
6. 请另一位已安装员工用自然语言查到这条知识；
7. 在一个真实大任务结束后执行知识沉淀检查，确认有候选或明确“无需提交”；
8. 把自检结果、查询结果、发布双 ID、跨员工查询结果和安全错误码（如有）反馈给测试负责人。

每个人都必须使用自己的账号。不得代替同事登录、共享密码/MFA，或使用管理员账号冒充其他员工。
测试通过表示该员工自己的安装和使用链路可用，不代表公司知识助手已经达到全公司正式发布状态。

## 三、开始前准备

你需要：

- 已安装并能正常打开的 Codex；
- 自己的 Memova Microsoft 工作账号，例如 `你的名字@memova.ai`；
- 能亲自完成该账号的密码和 MFA/两步验证。

你不需要：

- GitHub 账号或 GitHub 登录；
- `git clone`；
- SharePoint 管理员账号；
- 把任何密码或验证码发给 Codex、管理员或同事。

登录时使用的是你的 **Memova Microsoft 工作账号**，不是 GitHub 账号，也不是个人 ChatGPT 账号。

## 四、第一次安装或以后升级

### 第 1 步：把固定安装话术发给 Codex

打开公开仓库中的
[`INSTALL_PROMPT.md`](https://github.com/gxyfred/memova-company-knowledge-plugin/blob/main/INSTALL_PROMPT.md)，
复制标题下面的全部安装／升级话术，然后粘贴到一个 Codex 任务中发送。

这段话同时适用于第一次安装和以后升级。Codex 会：

1. 刷新公开 Marketplace；
2. 安装或升级公司知识助手 Plugin；
3. 检查专用连接 `company_knowledge_assistant`；
4. 如果尚未登录，启动 Microsoft 登录流程。

如果 Codex 打开了 Microsoft 登录页面，它会停下来等你。请你本人在浏览器中完成登录和 MFA。
不要把密码、验证码、Token、Cookie 或浏览器回调地址复制到对话里。

### 第 2 步：确认登录状态

登录完成后回到 Codex，告诉它：

> Microsoft 登录已完成，请继续检查公司知识助手连接状态。

正常结果应显示：

- Plugin 已安装并启用；
- 版本不低于 `0.4.4`；
- `company_knowledge_assistant` 为 `enabled`；
- 不再显示 `Not logged in`。

### 第 3 步：彻底重启 Codex

这是必做步骤，不能只关闭当前对话：

- macOS：彻底退出 Codex（可使用 `Command + Q`），再重新打开；
- Windows：彻底退出 Codex 后重新打开，不要只关掉当前任务。

重启后新建一个全新的 Codex 任务。不要继续使用登录前打开的旧任务。

### 第 4 步：运行入职自检

在新任务中发送：

> 开始公司知识助手入职自检

Codex 会检查安装版本、专用工具、Microsoft 委托身份、查询能力、提交能力、七种知识类型、来源
和快照规则，以及权限和敏感信息保护。它可能要求你确认显示的安全身份确实是你本人。

自检中的临时预览不会自动发布。任何真实发布仍必须先让你看完整预览，再由你单独确认。

自检通过后即可正常使用。如果有项目统一安排的测试事实，请只发布管理员提供且你本人核对过的
精确内容。

## 五、第一次查询怎么做

新建一个普通任务，直接用日常语言提问，不需要记工具名。例如：

> 公司目前关于录音分片上传的决定是什么？请给出来源、快照时间、适用范围和已知限制。

或者：

> 最近一次公司知识助手试点的结论是什么？哪些内容已经验证，哪些还不能确认？

一个可信回答应包含：

- 明确结论；
- 可以打开或追溯的来源；
- 证据是什么时间观察到的；
- 结论适用于谁、哪个项目或哪个版本；
- 冲突、缺口、过期风险或其他限制。

如果证据不足，助手应该明确说“无法确认”或 `insufficient_evidence`。这不等于事实一定不存在，
只表示当前可读知识中没有足够证据，不应让助手猜测。

## 六、怎样把知识发布到公司公共知识库

### 什么内容适合发布

适合发布的是已经完成、可复用、能被证据证明，而且适合公司内部公开的结论，例如：

- 已经落地的代码行为、接口或配置变化；
- 已审批的设计决定；
- 已确认的会议决定和行动项；
- 部署、回滚或事故结论；
- 项目状态、负责人或下一步变化；
- 可供同事复用的方法和操作经验；
- 已确认可共享的文件或媒体信息。

不适合直接发布：未完成草稿、个人猜测、临时调试输出、完整聊天记录、整个代码仓库、整份原始
日志，以及任何密码、Token、Cookie、MFA、私钥、连接字符串或未经授权的个人信息。

### 第 1 步：要求检查候选

把事实写清楚，并告诉助手包含和排除哪些证据。例如：

> @公司知识助手 请检查下面这项已完成工作是否应提交到公司公共知识库：
> [在这里写经过确认的事实、完成时间和适用范围]。
> 只使用当前任务中能够证明这项事实的最终结果和测试证据；排除密码、Token、Cookie、MFA、
> 原始日志、完整对话和无关内容。先评估并展示候选，不要自动创建预览或发布。

助手会给出评估结果。常见结果包括：

- `PREPARE_NOW`：证据足够，可以申请临时预览；
- `NEED_MORE_EVIDENCE`：还缺来源、版本、时间或关键证据；
- `SENSITIVE_OR_RESTRICTED`：内容含敏感或受限信息；
- `NO_SUBMIT`：没有值得进入公共知识库的实质变化；
- `CORRECTION_REQUIRED`：需要更正一条已经发布的旧知识。

第一次测试可以使用下面的模板，但方括号内容必须替换为本人实际验证过的结果：

> @公司知识助手 请准备将下面这条事实提交到公司公共知识库：
> [日期]，[姓名]在自己的电脑上完成了 Memova 公司知识助手 Plugin [实际版本] 的安装和
> Microsoft 委托登录；专用 MCP 已启用，本人安全身份读取成功。测试标识：
> CKP-EMPLOYEE-[姓名拼音]-[日期]-A。该记录只用于员工安装、跨用户检索和来源追溯测试，
> 不代表公司知识助手已经全公司正式发布。请归类为 `business_status`。证据只使用当前任务中的
> Plugin 版本、MCP 状态和本人安全身份读取结果；排除密码、MFA、Token、Cookie、OAuth 回调、
> 完整对话和无关内容。先评估并展示候选，不要自动创建预览或发布。

如果实际结果还没有成功，就不能把它写成“已完成”；应先修复安装，或如实记录当前阻塞状态。

### 第 2 步：批准创建临时预览

只有候选内容完全正确时才回复：

> 批准为上面这个精确候选创建预览，不处理其他内容。

预览属于你本人，有效期为 30 分钟。30 分钟只是给你检查和确认的时间，不是知识只能保存
30 分钟。预览过期后不会发布；正式发布的知识不会因为 30 分钟到期而消失。

### 第 3 步：逐项检查服务器最终预览

至少检查：

- 将发布的事实是否准确；
- 时间、版本、负责人和适用范围是否准确；
- 来源和证据是否真能证明结论；
- 排除内容中是否包含秘密、个人信息或无关材料；
- 知识类型和可见范围是否正确；
- `preview_id`、预览哈希和到期时间是否存在。

如果任何内容不对，回复“取消”或要求重新生成，不要确认发布。修改事实、证据或范围后，必须
重新创建预览。

### 第 4 步：单独确认正式发布

只有完整预览无误时，按助手给出的 `preview_id` 和哈希精确确认。例如：

> 我确认发布这个完整预览：preview_id 为 [填写预览 ID]，哈希为 [填写预览哈希]；
> 只提交这一个预览。

创建预览的批准不能代替这次最终确认。

### 第 5 步：确认发布成功

成功结果应至少包含：

- Published Knowledge ID；
- SubmissionReceipt ID；
- 发布状态为 `visible`；
- 可追溯来源或 SharePoint 记录；
- 当前提交人和所有者为你本人。

如果状态是 `index_pending`，表示正式记录可能已经写入，但尚未证明可以搜索。不要重复发布；让
助手根据 `operation_id` 做有界的只读状态检查。

## 七、发布后怎样验证同事能搜到

让另一位已经完成安装和登录的同事，在新的 Codex 任务里用正常工作语言提问。不要只搜索测试
标识；问题应模拟同事真实会问的方式。

例如，已发布内容是“某员工完成了某版本的接入验证”，可询问：

> 最近一次公司知识助手接入验证的结果是什么？Plugin、专用 MCP 和 Microsoft 委托身份读取
> 分别是什么状态？请给出来源和限制。

通过标准：回答找到了正确知识，来源可追溯，快照和适用范围正确，并且没有把一次员工或设备的
测试夸大为全公司正式发布或实时健康状态。

## 八、已发布内容写错了怎么办

不要要求删除或静默覆盖旧记录。发送：

> @公司知识助手 这条已发布知识需要更正。请基于 [旧 Knowledge ID 或 Receipt ID] 准备前向
> 更正候选；旧内容的问题是 [说明]，正确事实和证据是 [说明]。先展示候选，不要自动发布。

系统会保留原记录和审计链，再发布一条可追溯的更正关系。

## 九、大任务结束后的知识沉淀规则

### 当前统一测试规则

每当 Agent **彻底完成一个较大的任务** 后，都应该进行一次公司知识沉淀检查，但不是把每个任务
日志都自动写入知识库。

“较大的任务”通常包括：正式功能完成、接口或架构改变、部署或事故处理完成、明确的产品/设计
决定、重要业务状态改变，或产生了可供其他同事反复使用的方法。

安全流程应为：

1. Agent 自动检查当前任务是否产生了稳定、可复用、有证据、适合公司内部公开的结论；
2. 最多展示 3 条候选，也可以明确回答“无需提交”；
3. 不自动创建远程预览；
4. 员工核对候选后，明确批准才创建 30 分钟预览；
5. 员工再次核对服务器最终预览，并单独确认后才正式发布；
6. 任何秘密、个人信息、未完成草稿、推测和无证据结论都不得进入候选。

这样既能减少任务完成后忘记沉淀的问题，也不会让 Agent 把错误、噪声或敏感内容自动发布给全公司。

### 当前版本如何执行

Plugin `0.4.4` **不会仅因为任务完成就自动创建预览或发布知识**。自动沉淀检查尚未进入这个已发布
版本，因此当前全员测试期间，员工必须在大任务完成后发送下面这句话：

> 这个任务已经彻底完成。请执行公司知识沉淀检查：只检查当前任务中已完成、可复用、有稳定证据
> 且适合公司内部公开的结论；最多列出 3 条候选。不要自动创建预览或发布，先让我确认。没有合适
> 内容就明确说无需提交。

后续 Plugin 实现自动提醒后，员工不再需要手动发送这句话；但预览批准和最终发布确认仍必须由
员工本人完成。

## 十、常见问题

### 1. 显示 `Not logged in`

让 Codex 按固定安装话术运行 `codex mcp login company_knowledge_assistant`。浏览器打开后，由你
本人登录 Memova Microsoft 工作账号。登录后彻底重启 Codex 并新建任务。

### 2. 显示缺少 7 个工具或 `host_binding_missing`

先确认安装和登录都已完成，然后彻底退出 Codex，重新打开并新建任务。不要在旧任务里继续自检，
也不要改用普通 SharePoint 连接器。

### 3. Microsoft 页面显示 OAuth state 已过期

关闭那次已过期的登录页，回到 Codex，只重新执行一次专用 MCP 登录。不要把回调 URL 发给任何人，
不要连续循环登录。

### 4. 查询返回 `insufficient_evidence`

这不是登录失败。它表示当前可读正式知识中没有足够证据。换成更明确的自然语言问题，或请知识
负责人补充一条带来源和快照的正式知识；不要要求助手猜测。

### 5. 预览创建失败

不要猜字段、反复重试或重新发布相同内容。把发生时间、Plugin 版本、错误码、审计 ID、预览或
操作 ID 发给公司知识平台管理员；不要附带密码、Token、Cookie、MFA 或完整受限材料。

## 十一、需要帮助时提供什么

可以提供：

- 发生时间；
- 你自己的工作账号显示；
- Codex 和 Plugin 版本；
- `company_knowledge_assistant` 是 `enabled`、`Not logged in` 还是其他安全状态；
- 错误码、请求 ID、审计 ID、预览 ID 或操作 ID；
- 你预期发生什么、实际发生什么。

绝对不要提供：密码、MFA 验证码、OAuth code、Token、Cookie、私钥、连接字符串、完整回调地址，
或未经授权的公司/个人敏感数据。
